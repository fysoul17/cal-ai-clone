/speckit.specify ""

다음 기능 우선순위 (Top 5)

  1. Supabase 인증 시스템 (최우선)

  이유: 모든 사용자 데이터 기능의 기반. 이 없이는 대시보드, 로그 저장이 불가능
  - Login/Signup 페이지
  - Supabase Auth 연동
  - Protected routes middleware
  - 소셜 로그인 (Google)

  # speckit으로 시작
  /speckit.specify "Supabase authentication with email/password and Google OAuth"

  ---
  2. 데이터베이스 스키마 & Supabase 설정

  이유: PRD에 정의된 users, food_logs 테이블이 필요
  - Supabase 프로젝트 설정
  - DB 스키마 마이그레이션
  - Row Level Security (RLS) 정책
  - TypeScript 타입 생성

  /speckit.specify "Supabase database schema with users and food_logs tables including RLS policies"

  ---
  [x] 3. 사용자 대시보드 페이지

  이유: 인증 후 사용자가 랜딩하는 핵심 화면
  - 일일 칼로리 진행률 표시
  - 오늘 기록한 음식 타임라인
  - "음식 추가" (+) 버튼
  - 목표 대비 현황 요약

  /speckit.specify "Daily dashboard showing calorie progress, meal timeline, and add food button. bypass supabase auth for now. we will add later. Data should be fake data for now since we do not have database integrated yet. Focus on UI/UX for now, do not overengineering, make it intuitive UI/UX"

  ---
  4. 음식 사진 업로드 & 저장

  이유: 핵심 기능(AI 분석)의 전제 조건
  - 이미지 캡처/갤러리 선택 UI
  - Supabase Storage 연동
  - 이미지 압축/최적화
  - 업로드 진행률 표시

  /speckit.specify "Food photo upload with camera capture and gallery selection using Supabase Storage"

  ---
  5. AI 음식 분석 (n8n 연동)

  이유: 앱의 핵심 차별화 기능
  - n8n 웹훅 트리거 Server Action
  - Vision API 연동 (GPT-4o/Gemini)
  - 분석 결과 모달 UI
  - 로딩/에러 상태 처리

  /speckit.specify "AI food analysis using n8n webhook with GPT-4o vision API returning nutrition data"